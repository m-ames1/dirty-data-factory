## Summary

<!-- What changed and why. -->

## Checklist

- [ ] `uv run ruff check .`, `uv run ruff format --check .`, and `uv run pytest` pass locally
- [ ] No personal or sensitive content added — including in notes added to tracked files like `CLAUDE.md`
- [ ] Docs updated to match any behaviour or schema change (`docs/`, `README.md`)
